# One systemd service name per line. restart_service.sh refuses anything
# not listed here, independently of the ALLOWED_SERVICES check the Python
# server already does — defense in depth. Edit this on the VPS itself.
nginx
mariadb
